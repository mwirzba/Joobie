@model IEnumerable<Joobie.Models.JobModels.CVJobApplicationUser>

@{
    ViewData["Title"] = "Applied";
}

<h1>Applied</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.JobsId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CvName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.JobsId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CvName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeUser.Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
